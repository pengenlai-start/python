"""
身份运算符　——　用于比较两个对象的内存地址是否一致，是否是对同一个对象的引用
运算符
    is ——　判断两个标识符是不是引用同一个对象　eg. x is y <==> id(x) == id(y)
    is not ——　判断两个标识符是不是引用不同对象　eg. x is not y <==> id(x) != id(y)

is 与　== 的区别
    is　: 判断两个变量引用对象是否为同一个
    == : 判断引用变量的值是否相等

"""