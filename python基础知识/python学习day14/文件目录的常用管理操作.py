"""
文件/目录的常用管理操作 ---- 需要 os 模块
方法名
rename      os.rename(源文件名, 目标文件名)
remove      os.remove(文件名)
listdir     os.listdir(目录名) 目录列表
mkdir       os.mkdir(目录名)
rmdir       os.rmdir(目录名)
getcwd      os.getcwd() 获取当前目录
chdir       os.chdir(目标目录)  修改工作目录
path.isdir  os.path.isdir(文件路径) 判断是否是目录
"""